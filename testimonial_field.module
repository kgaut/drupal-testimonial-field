<?php

/**
 * Implements hook_theme().
 */
function testimonial_field_theme($existing, $type, $theme, $path) {
  $themes['testimonial'] = [
    'render element' => 'elements',
    'template' => 'testimonial',
    'variables' => [
      'content' => [],
    ],
  ];
  return $themes;
}